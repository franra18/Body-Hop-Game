public static class GameData
{
    // -1 indicará que es el inicio del juego y no hay nada guardado aún
    public static float tiempoGuardado = -1f; 
    public static bool personaje1Activo = true;
}